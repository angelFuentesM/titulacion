function () {
var formulario = getElementsByName('formulario')[0];
	elementos = formualrio.elements,
	boton = getElementById('btn');

var validar = function(){


};	

	formulario.addEventListener("submit", validar);
}